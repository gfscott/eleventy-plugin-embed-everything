module.exports = function(match, config) {
  /**
   * TODO
   * 1. Fetch the toot embed via oembed, using 11ty-fetch
   *    https://docs.joinmastodon.org/methods/oembed/
   * 2. Return the embed code
   */
  return "";
}